/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['demo-noonpost.assiagroupe.net', 'english.shivrupi.com', 'secure.gravatar.com'],
    },
}

module.exports = nextConfig
